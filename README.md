# mtproto-installer
MTProto installer (menu) for Ubuntu
# MTProto installer (menu) for Ubuntu

Установка MTProto-прокси (Docker) через интерактивное меню.

## Быстрая установка (1 команда)

```bash
sudo -i bash <(curl -fsSL https://raw.githubusercontent.com/Stels2024/mtproto-installer/main/mtproto_menu.sh)
